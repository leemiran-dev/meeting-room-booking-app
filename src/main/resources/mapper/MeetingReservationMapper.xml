<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.meetingroom.mapper.MeetingReservationMapper">

    <insert id="insertReservation" parameterType="MeetingReservation">
        INSERT INTO MEETING_RESERVATION (
            ROOM_ID, RESERVER_NAME, RESERVATION_DATE, START_TIME, END_TIME)
        VALUES (
            #{roomId}, #{reserverName}, #{reservationDate}, #{startTime}, #{endTime}
        )
    </insert>

    <select id="findByDate" resultType="meetingReservation">
        SELECT
            RESERVATION_ID, ROOM_ID, RESERVER_NAME, RESERVATION_DATE, START_TIME, END_TIME
        FROM MEETING_RESERVATION
        WHERE RESERVATION_DATE = #{reservationDate}
        ORDER BY START_TIME
    </select>

    <select id="countConflict" resultType="int">
        SELECT COUNT(*)
        FROM MEETING_RESERVATION
        WHERE ROOM_ID = #{roomId}
            AND RESERVATION_DATE = #{reservationDate}
            AND <![CDATA[ START_TIME < CAST(#{endTime} AS TIME) ]]>
            AND <![CDATA[ END_TIME > CAST(#{startTime} AS TIME) ]]>
    </select>

    <delete id="deleteReservation">
        DELETE FROM MEETING_RESERVATION
        WHERE RESERVATION_ID = #{reservationId}
    </delete>
</mapper>